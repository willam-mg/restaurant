meta {
  name: update
  type: http
  seq: 3
}

put {
  url: {{BASE_URL}}/configuraciones/1
  body: json
  auth: inherit
}

headers {
  Accept: application/json
}

body:json {
  {
      "precio_por_km": 0,
      "porcentaje_empresa": 0.03,
      "porcentaje": 0.05,
      "activo": true
  }
}

script:post-response {
  test("Status code is 200", function () {
      expect(res.getStatus()).to.equal(200);
  });
}

settings {
  encodeUrl: true
}
