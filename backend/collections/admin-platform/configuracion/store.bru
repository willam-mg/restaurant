meta {
  name: store
  type: http
  seq: 1
}

post {
  url: {{BASE_URL}}/configuraciones
  body: json
  auth: inherit
}

headers {
  Accept: application/json
}

body:json {
  {
      "precio_por_km": 0,
      "porcentaje_empresa": 0.03,
      "porcentaje": 0.05,
      "activo": false
  }
}

script:post-response {
  test("Status code is 200", function () {
      expect(res.getStatus()).to.equal(201);
  });
}

settings {
  encodeUrl: true
}
