meta {
  name: store
  type: http
  seq: 1
}

post {
  url: {{BASE_URL}}/brands
  body: json
  auth: inherit
}

headers {
  Accept: application/json
}

body:json {
  {
    "name": "example brand"
  }
}

settings {
  encodeUrl: true
}
