meta {
  name: update
  type: http
  seq: 3
}

put {
  url: {{BASE_URL}}/brands/1
  body: json
  auth: inherit
}

headers {
  Accept: application/json
}

body:json {
  {
    "name": "example brand updated"
  }
}

settings {
  encodeUrl: true
}
