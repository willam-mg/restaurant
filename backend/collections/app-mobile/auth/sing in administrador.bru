meta {
  name: sing in administrador
  type: http
  seq: 4
}

post {
  url: {{BASE_URL}}/auth/administrador
  body: json
  auth: inherit
}

headers {
  Accept: application/json
}

body:json {
  {
      "email":"admin@admin.com",
      "password":"12345678"
  }
}

script:post-response {
  test("Status code is 200", function () {
      expect(res.getStatus()).to.equal(200);
  });
}

settings {
  encodeUrl: true
}
