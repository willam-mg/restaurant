meta {
  name: store
  type: http
  seq: 1
}

post {
  url: {{BASE_URL}}/observaciones
  body: json
  auth: inherit
}

headers {
  Accept: application/json
}

body:json {
  {
      "attribute": "nombre",
      "observacion": "no es lo crrecto",
      "taxi_id": 1
  }
}

script:post-response {
  test("Status code is 200", function () {
      expect(res.getStatus()).to.equal(201);
  });
}

settings {
  encodeUrl: true
}
