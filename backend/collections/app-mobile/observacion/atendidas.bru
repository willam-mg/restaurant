meta {
  name: atendidas
  type: http
  seq: 9
}

get {
  url: {{BASE_URL}}/observaciones/atendidas
  body: none
  auth: inherit
}

headers {
  Accept: application/json
}

script:post-response {
  test("Status code is 200", function () {
      expect(res.getStatus()).to.equal(200);
  });
}

settings {
  encodeUrl: true
}
