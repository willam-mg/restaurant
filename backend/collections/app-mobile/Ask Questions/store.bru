meta {
  name: store
  type: http
  seq: 1
}

post {
  url: {{BASE_URL}}/ask-questions
  body: json
  auth: inherit
}

headers {
  Accept: application/json
}

body:json {
  {
    "question": "this is the cuestion",
    "answer": "this is the answer"
  }
}

settings {
  encodeUrl: true
}
